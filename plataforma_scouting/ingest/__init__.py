"""Capa de ingesta (ETL) — loaders por fuente de datos.

Este archivo SOLO marca el paquete `ingest`. NO debe ejecutar código de scraping:
todo lo que esté acá se corre cada vez que se importa el paquete (efecto colateral).
El código exploratorio va en `notebooks/` o en scripts con `if __name__ == "__main__"`.
"""
