"""app_scouting.py — Dashboard de scouting (Streamlit).

Réplica/superación del enfoque "Capitán FI": filtros (liga, posición, edad,
minutos, valor de mercado, pasaporte) + Scoring Index + tabla + radar del
jugador seleccionado.

Correr:
    streamlit run dashboards/app_scouting.py

⚠️ STUB — implementar en Fase 3 (cuando estén db + percentiles + scoring_index).
"""
from __future__ import annotations

# import streamlit as st
# import duckdb
# from metrics.scoring_index import calcular_scoring_index

# st.set_page_config(page_title="Scouting · datafutbol_ar", layout="wide")
# st.title("🔎 Scouting — datafutbol_ar")
# TODO: filtros en sidebar -> query DuckDB -> tabla ordenable -> radar del seleccionado.
